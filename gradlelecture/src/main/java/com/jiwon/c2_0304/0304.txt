- 자바는 반드시 클래스가 존재 한다.

- static

DTO , VO
    1) class
    2) 인스턴스 변수
    3) 생성자

함수가 실행 될때 this라는 변수가 생성된다.

자바스크립트에서

var a = function add(){

    }
var b = b();
window.b()

var b = new B();


---------------------
로직
    순수함수
        - Math.random
        - 주로 static 키워드를 붙여서 사용한다.
    순수 x
        -

리턴형 메소드
  -  리턴 타입의 메소드에서 는 이름 없는 변수가 생성된다.
    return 있는 값이나 변수가 대입된다.
  - 리턴 하면 함수가 종료한다.
  - 리턴은 한번만 나오는게 좋다. (0304_ArrayUtill 예제)

클래스
    - static 으로 구성된 순수함수들의 묶음
    - 데이터 + 로직 을 갖고 있는 묶음